#!/bin/bash
#
# Update boilerplate synth data from OPTs
#
USAGE="$0 <run_00N>"

if [[ $# -ne 1 ]]; then
	echo $USAGE
	exit 1
fi

s2 data boilerplate -s S2-models-demo/opts -o S2-test-data/synth -f $1 -n 10

