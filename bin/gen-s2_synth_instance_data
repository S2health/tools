#!/bin/bash
#
# Regenerate instance data from manually updated boilerplate data templates
#
USAGE="$0 <run_00N>"

if [[ $# -ne 1 ]]; then
	echo $USAGE
	exit 1
fi

s2 data instances -s S2-models-demo/opts -o S2-test-data/synth -f $1 -n 10


