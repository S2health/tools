#!/bin/bash
#
# model command to be executed in the directory containing cloned repos
#

lib_id=S2-models-demo
log_dir=S2-exported/$lib_id/logs
log_file=$log_dir/`basename $0`.log

# create log dir if it does not exist
mkdir -p $log_dir
if [ $? -ne 0 ] || [ ! -w $log_dir ]; then
	echo "Logs dir $log_dir doesn't exist or not writable"
	exit 1
fi

# main command
echo "adlc --cfg tools/cfg/adlc.cfg -b $lib_id --export --flat -f json -o S2-exported/$lib_id/json/flat" > $log_file
adlc --cfg tools/cfg/adlc.cfg -b $lib_id --export --flat -f json -o S2-exported/$lib_id/json/flat >> $log_file 2>&1
